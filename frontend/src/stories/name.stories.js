import Name from '../page/Name/Name.js';

export default {
    title: 'Widget/Name',
    component: Name,
}

const Template = (args) => {return(<Name/>)}

export const Default = Template.bind({});
Default.args = {
  };